@include('console.nav', ['title' => 'Layanan PKP'])
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><p class="text-secondary mb-0">Kelola judul layanan dan tautan Google Form atau situs layanan lainnya.</p><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#serviceCreate"><i class="fa fa-plus me-1"></i>Tambah Layanan</button></div>
<div class="card overflow-hidden"><div class="p-3 border-bottom d-flex justify-content-between align-items-center"><h2 class="h5 mb-0">Daftar Layanan</h2><span class="badge text-bg-secondary">{{ $services->total() }} layanan</span></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Judul</th><th>Link</th><th>Status</th><th>Urutan</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($services as $service)<tr><td><div class="fw-bold"><i class="fa {{ $service->icon }} me-2 text-primary"></i>{{ $service->title }}</div><small class="text-secondary">{{ \Illuminate\Support\Str::limit($service->description, 75) }}</small></td><td><a href="{{ $service->url }}" target="_blank" rel="noopener" class="small text-break">{{ \Illuminate\Support\Str::limit($service->url, 50) }} <i class="fa fa-arrow-up-right-from-square"></i></a></td><td><span class="badge text-bg-{{ $service->is_active ? 'success' : 'secondary' }}">{{ $service->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td>{{ $service->sort_order }}</td><td class="text-end"><button class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#service{{ $service->id }}"><i class="fa fa-pen"></i></button><form method="post" action="/console/layanan-pkp/{{ $service->id }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger js-swal-delete" data-message="Hapus layanan {{ $service->title }}?"><i class="fa fa-trash"></i></button></form></td></tr>@empty<tr><td colspan="5" class="text-center text-secondary py-5">Belum ada layanan. Tambahkan tautan Google Form melalui tombol Tambah Layanan.</td></tr>@endforelse</tbody></table></div>@if($services->hasPages())<div class="p-3 border-top">{{ $services->links() }}</div>@endif</div>
@include('console.pkp-service-form', ['id' => 'serviceCreate', 'action' => '/console/layanan-pkp', 'service' => null])
@foreach($services as $service)@include('console.pkp-service-form', ['id' => 'service'.$service->id, 'action' => '/console/layanan-pkp/'.$service->id, 'service' => $service])@endforeach
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
@include('console.end')
