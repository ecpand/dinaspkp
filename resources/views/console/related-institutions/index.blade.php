@include('console.nav', ['title' => 'Instansi Terkait'])
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><p class="text-secondary mb-0">Kelola logo dan tautan instansi terkait yang tampil pada beranda.</p><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#institutionCreate"><i class="fa fa-plus me-1"></i>Tambah Instansi</button></div>
<div class="card overflow-hidden"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Logo</th><th>Instansi</th><th>Link</th><th>Urutan</th><th>Status</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($institutions as $institution)<tr><td>@if($institution->logo_path)<img src="{{ asset('storage/'.$institution->logo_path) }}" alt="{{ $institution->name }}" class="border rounded p-1" style="width:58px;height:58px;object-fit:contain">@else<span class="text-secondary small">Belum ada</span>@endif</td><td><strong>{{ $institution->name }}</strong></td><td><a href="{{ $institution->website_url }}" target="_blank" class="small">{{ Str::limit($institution->website_url, 45) }}</a></td><td>{{ $institution->sort_order }}</td><td><span class="badge text-bg-{{ $institution->is_active ? 'success' : 'secondary' }}">{{ $institution->is_active ? 'Tayang' : 'Nonaktif' }}</span></td><td class="text-end"><button class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#institution{{ $institution->id }}"><i class="fa fa-pen"></i></button><form action="/console/instansi-terkait/{{ $institution->id }}" method="post" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger js-swal-delete" data-message="Hapus instansi {{ $institution->name }}?"><i class="fa fa-trash"></i></button></form></td></tr>@empty<tr><td colspan="6" class="text-center text-secondary py-5">Belum ada instansi terkait.</td></tr>@endforelse</tbody></table></div></div>
@include('console.related-institutions.form', ['id' => 'institutionCreate', 'action' => '/console/instansi-terkait', 'institution' => null])
@foreach($institutions as $institution)@include('console.related-institutions.form', ['id' => 'institution'.$institution->id, 'action' => '/console/instansi-terkait/'.$institution->id, 'institution' => $institution])@endforeach
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>@include('console.end')
