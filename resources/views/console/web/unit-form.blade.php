<div class="modal fade" id="{{ $id }}" tabindex="-1"><div class="modal-dialog"><form method="post" action="{{ $action }}" class="modal-content">@csrf @if($unit)@method('PUT')@endif<div class="modal-header"><h2 class="fs-5">{{ $unit ? 'Edit' : 'Tambah' }} Unit Struktur</h2><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body row g-3"><div class="col-12"><label class="form-label">Nama unit / jabatan</label><input name="name" class="form-control" value="{{ $unit?->name }}" required></div><div class="col-md-7"><label class="form-label">Kunci unit</label><input name="unit_key" class="form-control" value="{{ $unit?->unit_key }}" placeholder="Contoh: kabid-rumah-umum" required><small class="text-secondary">Gunakan huruf kecil, angka, dan tanda hubung.</small></div><div class="col-md-5"><label class="form-label">Urutan tampil</label><input type="number" min="0" name="sort_order" class="form-control" value="{{ $unit?->sort_order ?? 0 }}"></div><div class="col-12"><label class="form-label">Unit induk</label><select name="parent_key" class="form-select"><option value="">Tidak memiliki induk</option>@foreach($parentUnits as $parent)<option value="{{ $parent->unit_key }}" @selected($unit?->parent_key===$parent->unit_key) @disabled($unit?->id===$parent->id)>{{ $parent->name }} ({{ $parent->unit_key }})</option>@endforeach</select></div><div class="col-12"><input type="hidden" name="is_active" value="0"><div class="form-check form-switch"><input class="form-check-input" type="checkbox" name="is_active" value="1" id="activeUnit{{ $id }}" @checked(!$unit || $unit->is_active)><label class="form-check-label" for="activeUnit{{ $id }}">Tampilkan dalam struktur organisasi</label></div></div></div><div class="modal-footer"><button type="button" class="btn btn-light" data-bs-dismiss="modal">Batal</button><button class="btn btn-primary"><i class="fa fa-floppy-disk me-1"></i>Simpan Unit</button></div></form></div></div>
