@include('console.nav',['title'=>'Agenda Kegiatan'])
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="d-flex justify-content-between align-items-center mb-4"><p class="text-secondary mb-0">Jadwal kegiatan yang tampil pada halaman agenda frontend.</p><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#agendaCreate"><i class="fa fa-plus me-1"></i>Tambah Agenda</button></div>
<div class="card p-3"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Kegiatan</th><th>Tanggal</th><th>Lokasi</th><th>Status</th><th></th></tr></thead><tbody>@forelse($agendas as $agenda)<tr><td><strong>{{ $agenda->title }}</strong><br><small class="text-secondary">{{ Str::limit($agenda->description,70) }}</small></td><td>{{ $agenda->start_date?->format('d M Y') }}<br><small>{{ $agenda->start_time?->format('H:i') }}</small></td><td>{{ $agenda->location ?: '-' }}</td><td><span class="badge text-bg-primary">{{ ucfirst($agenda->status) }}</span></td><td><button class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#agenda{{ $agenda->id }}"><i class="fa fa-pen"></i></button><form method="post" action="/console/agenda/{{ $agenda->id }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Hapus agenda ini?')"><i class="fa fa-trash"></i></button></form></td></tr>@empty<tr><td colspan="5" class="text-center text-secondary py-4">Belum ada agenda.</td></tr>@endforelse</tbody></table></div></div>
@include('console.agenda-form',['id'=>'agendaCreate','action'=>'/console/agenda','agenda'=>null])@foreach($agendas as $agenda)@include('console.agenda-form',['id'=>'agenda'.$agenda->id,'action'=>'/console/agenda/'.$agenda->id,'agenda'=>$agenda])@endforeach<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>@include('console.end')
