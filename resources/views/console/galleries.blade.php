@include('console.nav',['title'=>'Galeri'])
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="d-flex justify-content-between align-items-center mb-4"><p class="text-secondary mb-0">Kelola Galeri Kegiatan, Galeri Informasi, dan Galeri Video.</p><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#galleryCreate"><i class="fa fa-plus me-1"></i>Tambah Galeri</button></div>
<div class="row g-4">@forelse($galleries as $gallery)@php($image=$gallery->image_path ? (str_starts_with($gallery->image_path,'assets/')?asset($gallery->image_path):asset('storage/'.$gallery->image_path)) : null)<div class="col-md-6 col-xl-4"><article class="card overflow-hidden h-100">@if($image)<img src="{{ $image }}" style="height:155px;object-fit:cover" class="w-100">@else<div class="d-grid place-items-center bg-light text-primary" style="height:155px"><i class="fa fa-play-circle fa-3x"></i></div>@endif<div class="p-3"><span class="badge text-bg-info">{{ ucfirst($gallery->category) }}</span><span class="badge text-bg-{{ $gallery->is_active?'success':'secondary' }}">{{ $gallery->is_active?'Aktif':'Nonaktif' }}</span><h2 class="h6 mt-2">{{ $gallery->title }}</h2><p class="small text-secondary">{{ Str::limit($gallery->description,80) }}</p><button class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#gallery{{ $gallery->id }}"><i class="fa fa-pen"></i> Edit</button><form method="post" action="/console/galeri/{{ $gallery->id }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Hapus item galeri ini?')"><i class="fa fa-trash"></i></button></form></div></article></div>@empty<div class="col-12"><div class="card p-5 text-center text-secondary">Belum ada data galeri.</div></div>@endforelse</div>
@include('console.gallery-form',['id'=>'galleryCreate','action'=>'/console/galeri','gallery'=>null])@foreach($galleries as $gallery)@include('console.gallery-form',['id'=>'gallery'.$gallery->id,'action'=>'/console/galeri/'.$gallery->id,'gallery'=>$gallery])@endforeach<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>@include('console.end')
