@include('console.nav',['title'=>'Dokumen Publik'])
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<div class="d-flex justify-content-between align-items-center mb-4"><p class="text-secondary mb-0">Kelola dokumen Peraturan, Informasi, dan seluruh file Unduhan publik.</p><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#documentCreate"><i class="fa fa-upload me-1"></i>Tambah Dokumen</button></div>
<div class="card p-3"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Dokumen</th><th>Kategori</th><th>Tanggal</th><th>File</th><th></th></tr></thead><tbody>@forelse($documents as $document)@php($fileUrl=str_starts_with($document->file_path,'assets/')?asset($document->file_path):asset('storage/'.$document->file_path))<tr><td><strong>{{ $document->title }}</strong><br><small class="text-secondary">{{ Str::limit($document->description,80) }}</small></td><td><span class="badge text-bg-info">{{ ucfirst($document->category) }}</span></td><td>{{ optional($document->published_date)->format('d M Y') ?: '-' }}</td><td><a href="{{ $fileUrl }}" target="_blank" class="btn btn-sm btn-outline-secondary"><i class="fa fa-download"></i></a></td><td><button class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#document{{ $document->id }}"><i class="fa fa-pen"></i></button><form method="post" action="/console/dokumen/{{ $document->id }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Hapus dokumen ini?')"><i class="fa fa-trash"></i></button></form></td></tr>@empty<tr><td colspan="5" class="text-center text-secondary py-4">Belum ada dokumen.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $documents->links() }}</div>
@include('console.document-form',['id'=>'documentCreate','action'=>'/console/dokumen','document'=>null])@foreach($documents as $document)@include('console.document-form',['id'=>'document'.$document->id,'action'=>'/console/dokumen/'.$document->id,'document'=>$document])@endforeach<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>@include('console.end')
