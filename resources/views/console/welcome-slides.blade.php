@include('console.nav',['title'=>'Welcome Slide'])
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="d-flex justify-content-between align-items-center mb-4"><p class="text-secondary mb-0">Kelola gambar sambutan yang tampil pada beranda.</p><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#slideForm"><i class="fa fa-plus me-1"></i>Tambah Slide</button></div>
<div class="row g-4">@forelse($slides as $slide)<div class="col-md-6 col-xl-4"><article class="card overflow-hidden h-100"><img src="{{ $slide->image_url }}" class="w-100" style="height:180px;object-fit:cover"><div class="p-3"><span class="badge text-bg-{{ $slide->is_active?'success':'secondary' }}">{{ $slide->is_active?'Aktif':'Nonaktif' }}</span><h2 class="h6 mt-2 mb-1">{{ $slide->title }}</h2><p class="small text-secondary mb-3">{{ $slide->subtitle }}</p><div class="d-flex gap-2"><button class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#editSlide{{ $slide->id }}"><i class="fa fa-pen"></i> Edit</button><form method="post" action="/console/slider-sambutan/{{ $slide->id }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger js-swal-delete" data-message="Hapus slide {{ $slide->title }}?"><i class="fa fa-trash"></i></button></form></div></div></article></div>@empty<div class="col-12"><div class="card p-5 text-center text-secondary">Belum ada welcome slide.</div></div>@endforelse</div>
@include('console.welcome-slide-form',['id'=>'slideForm','action'=>'/console/slider-sambutan','slide'=>null])
@foreach($slides as $slide)@include('console.welcome-slide-form',['id'=>'editSlide'.$slide->id,'action'=>'/console/slider-sambutan/'.$slide->id,'slide'=>$slide])@endforeach
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script><script>document.querySelectorAll('.js-slide-image').forEach(input=>input.addEventListener('change',function(){const file=this.files?.[0];const preview=document.getElementById(this.dataset.preview);if(file&&preview)preview.src=URL.createObjectURL(file)}));</script>@include('console.end')
